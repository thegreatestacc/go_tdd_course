# Learn Go with Tests: progress

Мои решения упражнений из курса
[Learn Go with Tests](https://quii.gitbook.io/learn-go-with-tests).

> Последнее обновление: 12 июня 2026 года

## Текущий результат

| Метрика | Результат |
|---|---:|
| Пройдено основной программы | **4 из 31 глав (12,9%)** |
| Пройдено раздела Go fundamentals | **4 из 20 глав (20%)** |
| Качество текущих решений | **76 / 100** |
| Решено практических этапов | **7** |

Прогресс основной программы:

```text
[####---------------------------] 12,9%
```

## Пройденные главы

| № | Глава | Статус | Реализовано |
|---:|---|---|---|
| 1 | Hello, World | Завершена | Приветствия на нескольких языках, helper для тестов |
| 2 | Integers | Завершена | `Add` |
| 3 | Iteration | Завершена | `Repeat`, benchmark |
| 4 | Arrays and slices | Завершена | `Sum`, `SumAll`, `SumAllTails` |
| 5 | Structs, methods & interfaces | Следующая | — |

Решения находятся в каталоге [`solutions`](solutions).

## Оценка качества

Оценка отражает текущее состояние решений, а не только факт прохождения
существующих тестов.

| Критерий | Балл | Комментарий |
|---|---:|---|
| Корректность | 31 / 40 | Основные сценарии работают; `Repeat` игнорирует аргумент `count`, а неизвестный язык в `Hello` не использует английское приветствие |
| Тесты | 24 / 30 | Есть subtests, test helper и проверка пустого tail; не хватает тестов, обнаруживающих указанные дефекты |
| Go-стиль | 17 / 20 | Код проходит `gofmt`, используются `strings.Builder`, `slices.Equal` и idiomatic range |
| Структура проекта | 4 / 10 | Большинство реализаций находятся в файлах `_test.go`; `b.Loop()` требует Go 1.24+, но в `go.mod` указан Go 1.23 |
| **Итого** | **76 / 100** | Хорошая основа, но перед следующими главами стоит исправить найденные дефекты и структуру файлов |

## Проверка

Команда для запуска всех решений:

```powershell
go test ./solutions/...
```

На Go 1.23 четыре из пяти пакетов проходят тесты. Пакет `3_iteration`
не собирается, потому что benchmark использует `b.Loop()`, добавленный в
Go 1.24.

## Как считается прогресс

В основную программу включены практические главы из разделов
**Go fundamentals**, **Testing fundamentals** и **Build an application**:
всего 31 глава. Установочные, справочные и meta-главы в процент прогресса
не входят.
